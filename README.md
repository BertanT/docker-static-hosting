# docker-static-hosting
Cute little setup for hardening safety with hosting potentially vulnerable static websites using docker. Implement read-only bind mounts to isolate the server from site source.
